#include <stdio.h>

int main() {
    int a, b, sum;

    // Read input values for a and b
    scanf("%d", &a);
    scanf("%d", &b);

    // Calculate the sum
    sum = a + b;

    // Print the result
    printf("Addition of two Numbers:%d\n", sum);

    return 0;
}
